# Day 001 — Environment Setup + Floating-Point Foundations

**Date:** ___________
**Hours spent:** ___________
**Phase:** 1 — GPU + ML Foundations
**Goal today:** Get the full dev environment running, verify GPU access from CUDA and Python,
and begin understanding floating-point number representation.

---

## What I planned

- [ ] Install / verify CUDA Toolkit 12.x
- [ ] Verify GPU with `nvidia-smi` and `nvcc --version`
- [ ] Set up Python venv and install PyTorch with CUDA
- [ ] Build the project with CMake and run `test_quantize`
- [ ] Read: What is IEEE 754? What is FP32 vs FP16?

---

## What I actually did

> Write this section at the end of the day. Be honest — if setup took longer than
> expected, say so. This is where you capture what really happened vs the plan.
> These notes become your paper's "Implementation" section later.

...

---

## Environment check log

Paste the output of these commands here. This serves as your baseline record
and helps debug issues in later days.

```
$ nvidia-smi
[paste output]

$ nvcc --version
[paste output]

$ python -c "import torch; print(torch.cuda.get_device_name(0)); print(torch.version.cuda)"
[paste output]

$ ./build/benchmark 1000000
[paste output]
```

---

## Concepts learned today

> For each concept: write a 2–4 sentence explanation IN YOUR OWN WORDS.
> If you can't explain it simply, you haven't understood it yet — look it up again.
> These explanations are the raw material for the Glossary in the README.

### IEEE 754 FP32

...

### What FP16 / BF16 trade off vs FP32

...

### Why GPUs prefer lower precision

...

---

## Code written / modified

> List every file you touched and what you changed. Include the key insight
> from each file, not just the filename.

- `cuda_kernels/src/benchmark.cu` — ran for the first time. Observed: ...
- ...

---

## Profiler observations

> After any kernel run, note the numbers. Even on Day 1 you can look at the
> benchmark output. Get into the habit of recording numbers, not impressions.

| Metric             | Value         | Notes                      |
|--------------------|---------------|----------------------------|
| Kernel runtime     |               | from cudaEvent timing      |
| Throughput (GB/s)  |               | from benchmark output      |
| vs theoretical max |               | RTX 4060: ~272 GB/s        |

---

## Surprises / unexpected results

> This is the most important section. Things that didn't match your expectation
> are where the real learning happens. At NVIDIA, engineers are judged by how
> well they understand anomalies in data.

...

---

## Open questions

> Things you don't understand yet. Keep a running list. Try to answer at least
> one per day.

1. ...
2. ...

---

## Tomorrow's plan (Day 2)

- [ ] ...
- [ ] ...
- [ ] ...

---

*Template: copy this file to day_002.md, day_003.md, etc. Update the header.*
*Tip: commit your journal files to git every day. The commit history is a timestamp.*
